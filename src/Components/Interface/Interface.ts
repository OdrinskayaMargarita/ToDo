export interface objTask {
  id: string,
  title: string,
  isCompleted: boolean
}

export interface objTaskNote {
  id: number,
  title: String,
  isCompleted: boolean
}

export interface listItem {
  id: number,
  title: string,
  isCompleted: boolean
}
